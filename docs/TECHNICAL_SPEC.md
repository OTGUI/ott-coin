# 🎎 $OTTOGI 기술 설계서

> **"넘어져도 다시 일어난다"** — 세상에서 가장 따뜻한 밈코인

**Version:** 2.0  
**Date:** 2026-02-19  
**Chain:** Solana  

---

## 1. 프로젝트 개요

### 기본 정보

| 항목 | 내용 |
|------|------|
| 토큰명 | OTTOGI |
| 티커 | $OTTOGI |
| 체인 | Solana (SPL Token) |
| 표준 | SPL Token-2022 |
| 컨셉 | 오뚜기 (Tumbling Doll) — 넘어져도 다시 일어남 |
| 카테고리 | 감성 유틸리티 밈코인 |

### 컨셉

오뚜기(不倒翁)는 쓰러뜨려도 다시 일어나는 인형이다. 크립토 시장에서 -90% 하락을 겪어도 다시 일어나는 홀더들, 인생에서 넘어져도 다시 일어나는 모든 사람들을 응원하는 코인이다.

보유자에게는 자동으로 **응원 NFT**가 에어드롭된다. "오늘도 수고했어", "넘어져도 괜찮아" 같은 메시지가 담긴 NFT를 통해 홀더들에게 정서적 가치를 제공한다.

### 슬로건 후보

1. **"Fall Seven Times, Stand Up Eight"** (七転八起)
2. **"넘어져도 괜찮아, 다시 일어나면 돼"**
3. **"Every Dip is a Setup for a Comeback"**
4. **"We Don't Stay Down"**
5. **"오뚜기처럼, 다시. 🎎"**

---

## 2. 토큰 이코노미

### 총 발행량

**2,430,000,000,000 (2조 4,300억) $OTTOGI**

> **발행량의 의미:**
> - **8,100,000,000** (현재 지구 인구 81억)
> - **× 100** (완전한, 온전한 수 — 100% 응원)
> - **× 3** (동양 속담: "인생에 3번은 기회가 온다")
> - = **2,430,000,000,000개** — 지구의 모든 사람에게 300번의 기회를

Decimals: 6. 고정 발행, 추가 발행 없음.

### 분배 구조

| 항목 | 비율 | 수량 | 용도 |
|------|------|------|------|
| 유동성 풀 (LP) | 85% | 2,065,500,000,000 | Pump.fun 본딩커브 → Raydium LP |
| 에어드롭 풀 | 5% | 121,500,000,000 | NFT 민팅 가스비 + 이벤트 |
| 팀 | 5% | 121,500,000,000 | 개발/운영 (6개월 락업) |
| 마케팅 | 3% | 72,900,000,000 | KOL, 이벤트, 콘텐츠 |
| 커뮤니티 리저브 | 2% | 48,600,000,000 | DAO 투표 등 미래 용도 |

> **참고:** Pump.fun 발행 시 초기에는 100%가 본딩커브에 들어가므로, 팀/마케팅 물량은 별도 월렛에서 시장 매수 후 확보하거나, Pump.fun 졸업 후 별도 분배한다.

### Pump.fun 본딩커브 구조

Pump.fun은 **상수곱 본딩커브(Constant Product Bonding Curve)**를 사용한다:

```
가격 = k / (총공급량 - 판매량)²
```

- **초기 가격:** ~$0.000000001 수준에서 시작
- **졸업 조건:** 시가총액이 약 **$69,000** (약 420 SOL)에 도달
- **졸업 시:** 본딩커브에 남은 SOL이 자동으로 Raydium LP로 이전
- **LP 토큰:** 자동 소각 (burn) → 러그풀 방지

### Raydium 자동 상장 조건

1. Pump.fun 본딩커브가 졸업 조건 달성 (시총 ~$69K)
2. 약 79 SOL + 남은 토큰으로 Raydium CPMM 풀 자동 생성
3. LP 토큰 즉시 소각
4. 이후 자유 시장 가격 결정

---

## 3. NFT 시스템 상세 설계

### 등급 체계

| 등급 | 아이콘 | 발행량 | 기본 확률 | 콘텐츠 | 설명 |
|------|--------|--------|----------|--------|------|
| 일반 (Common) | ⚪ | 무제한 | 80% | 짧은 응원 메시지 | "오늘도 힘내!" 등 1줄 |
| 희귀 (Rare) | 🔵 | 무제한 | 15% | 긴 메시지 | 2~3문장 응원 메시지 |
| 영웅 (Epic) | 🟣 | 무제한 | 4.5% | 특수문자/강조 포함 | 꾸며진 응원 카드 |
| 전설 (Legendary) | 🟡 | **40개 한정** | 0.48% | AI 이미지 + 유명인 명언 | 동양풍 20개 + 서양풍 20개, 고유 아트워크 |
| 유일 (Mythic) | 🔴 | **3개 한정** | 0.02% | 궁극의 AI 아트워크 + 명언 | 인생의 3번의 기회, 최고 희소성 |

> **전설 등급 (40개 한정):**
> - 동양풍 20개: 한국 전통 오뚜기 인형 스타일, 동양 명언/속담
> - 서양풍 20개: 위블/다루마/펀칭백 스타일, 서양 명언
> - 각각 고유한 AI 생성 이미지 + 고유 명언 조합
> - 사전 제작 후 특별 이벤트/랜덤으로 에어드롭
>
> **유일 등급 (3개 한정):**
> - "인생에 3번은 기회가 온다" — 동양 속담에서 착안
> - 3개 모두 완전히 다른 스타일/메시지/아트워크
> - 최고 보유자 또는 특별 이벤트를 통해서만 획득 가능
> - 소유자에게 프로젝트 거버넌스 투표권 부여

### NFT 메타데이터 구조 (Metaplex 표준)

```json
{
  "name": "Ottogi Cheer #12345",
  "symbol": "OTTOGI-NFT",
  "description": "넘어져도 다시 일어나는 당신을 응원합니다 🎎",
  "image": "https://arweave.net/{TX_ID}",
  "animation_url": null,
  "external_url": "https://ottogi.fun/nft/12345",
  "attributes": [
    {
      "trait_type": "Grade",
      "value": "Legendary"
    },
    {
      "trait_type": "Grade_Icon",
      "value": "🟡"
    },
    {
      "trait_type": "Message",
      "value": "Fall seven times, stand up eight. - Japanese Proverb"
    },
    {
      "trait_type": "Category",
      "value": "proverb"
    },
    {
      "trait_type": "Language",
      "value": "en"
    },
    {
      "trait_type": "Airdrop_Round",
      "value": "42"
    },
    {
      "trait_type": "Recipient_Holding",
      "value": "1000000"
    }
  ],
  "properties": {
    "files": [
      {
        "uri": "https://arweave.net/{TX_ID}",
        "type": "image/png"
      }
    ],
    "category": "image",
    "creators": [
      {
        "address": "OTTOGI_AUTHORITY_PUBKEY",
        "share": 100
      }
    ]
  },
  "collection": {
    "name": "Ottogi Cheers",
    "family": "Ottogi"
  }
}
```

### 메시지 DB 설계

```sql
CREATE TABLE cheer_messages (
    id          INTEGER PRIMARY KEY,
    grade       ENUM('common', 'rare', 'epic', 'legendary', 'mythic'),
    category    VARCHAR(50),     -- motivation, proverb, humor, life, comeback
    language    VARCHAR(5),      -- ko, en, ja, zh
    message     TEXT NOT NULL,
    source      VARCHAR(200),    -- 출처 (인물명 등, nullable)
    used_count  INTEGER DEFAULT 0,
    created_at  TIMESTAMP
);

-- 초기 데이터 규모
-- common:   500+ 메시지
-- rare:     200+ 메시지
-- epic:     100+ 메시지
-- legendary: 50+ 명언
-- mythic:   프롬프트 템플릿 20+
```

**카테고리 분류:**
- `motivation` — 동기부여 ("할 수 있어!")
- `proverb` — 속담/격언
- `humor` — 유머 응원 ("손절은 미래의 나에게")
- `life` — 인생 조언
- `comeback` — 반등/재기 메시지

### 이미지 생성 파이프라인 (전설 + 유일 등급)

**전설 등급 (40개) — 사전 제작:**
```
[20개 동양풍 프롬프트] → [AI 이미지 생성] → [명언 텍스트 오버레이] → [Arweave 업로드]
[20개 서양풍 프롬프트] → [AI 이미지 생성] → [명언 텍스트 오버레이] → [Arweave 업로드]
                                                                        ↓
                                                               [40개 메타데이터 사전 생성]
```

**유일 등급 (3개) — 최고 퀄리티 사전 제작:**
```
[3개 궁극 프롬프트] → [AI 이미지 생성 (최고 해상도)] → [수작업 보정]
                                                          ↓
                                              [명언 + 아트 디렉션] → [Arweave 업로드]
```

- **동양풍 스타일:** 수묵화, 민화, 우키요에, 선(禪) 아트, 수채화 등
- **서양풍 스타일:** 팝아트, 네온, 3D, 픽셀아트, 수채화, 미니멀 등
- **유일 3개:** 각각 완전히 다른 아트 디렉션 (예: 우주적, 불사조, 궁극의 평화)
- **API:** OpenAI gpt-image-1 ($0.04/image) 또는 셀프호스팅 Stable Diffusion
- **총 이미지 제작:** 43개 (전설 40 + 유일 3) — 1회성, 사전 제작

### 등급 결정 알고리즘

```python
import random

def determine_grade(holder_balance: int, holding_days: int) -> str:
    """
    보유량과 보유기간에 따라 가중치를 적용한 등급 결정
    """
    # 기본 확률
    base_weights = {
        'common':    60,
        'rare':      20,
        'epic':      12,
        'legendary':  7,
        'mythic':     1,
    }
    
    # 보유량 보너스 (많이 보유할수록 높은 등급 확률 UP)
    if holder_balance >= 10_000_000:      # 1% 이상 보유
        bonus_multiplier = 3.0
    elif holder_balance >= 1_000_000:     # 0.1% 이상
        bonus_multiplier = 2.0
    elif holder_balance >= 100_000:       # 0.01% 이상
        bonus_multiplier = 1.5
    else:
        bonus_multiplier = 1.0
    
    # 보유기간 보너스
    if holding_days >= 90:
        bonus_multiplier *= 1.5
    elif holding_days >= 30:
        bonus_multiplier *= 1.2
    
    # 가중치 조정 (common 감소, 나머지 증가)
    adjusted = {}
    for grade, weight in base_weights.items():
        if grade == 'common':
            adjusted[grade] = weight / bonus_multiplier
        else:
            adjusted[grade] = weight * bonus_multiplier
    
    # 정규화 후 선택
    total = sum(adjusted.values())
    roll = random.uniform(0, total)
    
    cumulative = 0
    for grade, weight in adjusted.items():
        cumulative += weight
        if roll <= cumulative:
            return grade
    
    return 'common'
```

---

## 4. 자동 에어드롭 메커니즘

### 보유자 스캔 방법

Solana RPC의 `getTokenAccountsByMint`를 사용하여 $OTTOGI 토큰 보유자 목록을 스캔한다.

```typescript
import { Connection, PublicKey } from '@solana/web3.js';

async function getHolders(mintAddress: string): Promise<HolderInfo[]> {
  const connection = new Connection('https://api.mainnet-beta.solana.com');
  const mint = new PublicKey(mintAddress);
  
  const accounts = await connection.getParsedProgramAccounts(
    new PublicKey('TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'), // TOKEN_PROGRAM_ID
    {
      filters: [
        { dataSize: 165 },
        { memcmp: { offset: 0, bytes: mint.toBase58() } }
      ]
    }
  );
  
  return accounts
    .map(acc => ({
      owner: acc.account.data.parsed.info.owner,
      balance: acc.account.data.parsed.info.tokenAmount.uiAmount,
    }))
    .filter(h => h.balance > 0)
    .sort((a, b) => b.balance - a.balance);
}
```

### 에어드롭 자격 & 빈도

**최소 보유량: 10,000 $OTTOGI** (전체 발행량의 0.000000412%)

빈도와 등급 보너스는 **전체 발행량(2.43조) 대비 보유 비율**로 산출한다.

| 티어 | 보유량 | 전체 대비 비율 | 빈도 | 등급 보너스 |
|------|--------|---------------|------|------------|
| 🌱 씨앗 | 10,000 ~ 99,999 | ~0.000004% | 주 1회 (일) | ×1.0 (기본) |
| 🌿 새싹 | 100,000 ~ 999,999 | ~0.00004% | 주 2회 (수/일) | ×1.5 |
| 🌳 나무 | 1,000,000 ~ 99,999,999 | ~0.004% | 주 3회 (월/수/금) | ×2.5 |
| 🏔 산 | 100,000,000 ~ 9,999,999,999 | ~0.4% | 주 5회 (평일) | ×4.0 |
| 🌍 대지 | ≥ 10,000,000,000 | ~0.4%+ | 매일 | ×6.0 |

> **10,000개 미만 보유자는 NFT를 받지 못합니다.** 이는 스팸 방지 + 진정한 홀더 보상 목적입니다.
> 10,000 $OTTOGI는 Pump.fun 초기 가격 기준 $0.01 미만으로, 진입 장벽이 매우 낮습니다.

### NFT 등급 확률 (랜덤 결정)

매 에어드롭 시 등급은 **가중 랜덤**으로 결정된다. 기본(씨앗 티어) 확률:

| 등급 | 기본 확률 | 🌍대지 티어 (×6) |
|------|----------|-----------------|
| ⚪ 일반 (Common) | 80.0% | 44.0% |
| 🔵 희귀 (Rare) | 15.0% | 27.0% |
| 🟣 영웅 (Epic) | 4.5% | 21.0% |
| 🟡 전설 (Legendary) | 0.48% | 7.2% |
| 🔴 유일 (Mythic) | 0.02% | 0.8% |

> **등급 보너스 적용 공식:**
> 상위 등급 당첨 확률 = 기본확률 × 등급보너스배수 (단, 합계 100% 초과 시 Common에서 차감)
> 전설/유일은 **잔여 한정 수량** 내에서만 당첨 (전설 40개, 유일 3개 소진 시 하위 등급으로 대체)

### 보유 비율 기반 계산 로직

```typescript
const TOTAL_SUPPLY = 2_430_000_000_000; // 2.43조

interface AirdropTier {
  name: string;
  emoji: string;
  minBalance: number;
  minRatio: number;          // 최소 보유 비율 (balance / totalSupply)
  frequency: 'daily' | '5x_week' | '3x_week' | '2x_week' | 'weekly';
  gradeBonus: number;        // 상위 등급 확률 배수
}

const TIERS: AirdropTier[] = [
  { name: '대지', emoji: '🌍', minBalance: 10_000_000_000, minRatio: 0.004115, frequency: 'daily',    gradeBonus: 6.0 },
  { name: '산',   emoji: '🏔',  minBalance: 100_000_000,    minRatio: 0.000041, frequency: '5x_week',  gradeBonus: 4.0 },
  { name: '나무', emoji: '🌳', minBalance: 1_000_000,       minRatio: 0.0000004, frequency: '3x_week', gradeBonus: 2.5 },
  { name: '새싹', emoji: '🌿', minBalance: 100_000,         minRatio: 0.00000004, frequency: '2x_week', gradeBonus: 1.5 },
  { name: '씨앗', emoji: '🌱', minBalance: 10_000,          minRatio: 0.000000004, frequency: 'weekly', gradeBonus: 1.0 },
];

// 등급 결정 (가중 랜덤)
const BASE_RATES = {
  common: 0.80, rare: 0.15, epic: 0.045, legendary: 0.0048, mythic: 0.0002
};

function determineGrade(gradeBonus: number, legendaryLeft: number, mythicLeft: number): string {
  const boosted = {
    mythic:    Math.min(BASE_RATES.mythic * gradeBonus, 0.05),
    legendary: Math.min(BASE_RATES.legendary * gradeBonus, 0.10),
    epic:      Math.min(BASE_RATES.epic * gradeBonus, 0.30),
    rare:      Math.min(BASE_RATES.rare * gradeBonus, 0.35),
    common:    0  // 나머지
  };
  boosted.common = 1 - boosted.mythic - boosted.legendary - boosted.epic - boosted.rare;

  // 한정 수량 체크
  if (mythicLeft <= 0) { boosted.common += boosted.mythic; boosted.mythic = 0; }
  if (legendaryLeft <= 0) { boosted.common += boosted.legendary; boosted.legendary = 0; }

  const roll = Math.random();
  let cumulative = 0;
  for (const [grade, prob] of Object.entries(boosted)) {
    cumulative += prob;
    if (roll < cumulative) return grade;
  }
  return 'common';
}

function getTier(balance: number): AirdropTier | null {
  if (balance < 10_000) return null; // 자격 미달
  const ratio = balance / TOTAL_SUPPLY;
  return TIERS.find(t => balance >= t.minBalance) || TIERS[TIERS.length - 1];
}
```

### NFT 민트 + 전송 플로우

```
[Cron Trigger] → [보유자 스캔] → [등급 결정] → [메시지 선택]
                                                     ↓
                                              [유일등급?] → Yes → [AI 이미지 생성]
                                                     ↓ No              ↓
                                              [템플릿 이미지 선택] ← ──┘
                                                     ↓
                                              [메타데이터 JSON 생성]
                                                     ↓
                                              [Arweave/IPFS 업로드]
                                                     ↓
                                              [cNFT 민트 (Bubblegum)]
                                                     ↓
                                              [보유자 월렛으로 전송]
                                                     ↓
                                              [DB 기록 + 로그]
```

### 가스비 최적화 전략

1. **Compressed NFT (cNFT) 사용** — 일반 NFT 대비 가스비 1000x 절약
   - 일반 NFT 민트: ~0.01 SOL
   - cNFT 민트: ~0.00001 SOL
2. **배치 처리** — 여러 cNFT를 하나의 트랜잭션으로 묶기
3. **Priority Fee 동적 조절** — 네트워크 혼잡도에 따라 조절
4. **Merkle Tree 사전 생성** — 큰 트리를 미리 만들어 두고 잎 노드만 추가

### Cron/스케줄러 설계

```yaml
# cron 스케줄 (모든 시간 UTC, 한국 21:00 = UTC 12:00)
holder_snapshot:
  schedule: "0 11 * * *"         # 매일 11:00 UTC — 에어드롭 1시간 전 스냅샷
  task: "scan holders ≥ 10,000 $OTTOGI, calculate tiers by ratio"

airdrop_daily:
  schedule: "0 12 * * *"         # 매일 — 🌍 대지 티어 (≥100억)
  target: "daily tier"

airdrop_5x_week:
  schedule: "0 12 * * 1-5"      # 평일 — 🏔 산 티어 (≥1억)
  target: "5x_week tier"

airdrop_3x_week:
  schedule: "0 12 * * 1,3,5"    # 월/수/금 — 🌳 나무 티어 (≥100만)
  target: "3x_week tier"

airdrop_2x_week:
  schedule: "0 12 * * 3,0"      # 수/일 — 🌿 새싹 티어 (≥10만)
  target: "2x_week tier"

airdrop_weekly:
  schedule: "0 12 * * 0"        # 일요일 — 🌱 씨앗 티어 (≥1만)
  target: "weekly tier"
```

**스케줄러 구현:** Node.js + `node-cron` 또는 시스템 crontab. 프로덕션에서는 AWS Lambda + EventBridge 또는 Railway cron 권장.

---

## 5. 스마트 컨트랙트/프로그램

### Solana Program 필요 여부

| 기능 | 커스텀 프로그램 필요? | 대안 |
|------|----------------------|------|
| 토큰 발행 | ❌ | Pump.fun / SPL Token CLI |
| NFT 민트 | ❌ | Metaplex Bubblegum SDK |
| 에어드롭 | ❌ | 오프체인 스크립트 + RPC |
| 토큰 게이팅 | ⚠️ 선택 | 오프체인 검증으로 대체 가능 |
| DAO 거버넌스 | ⚠️ Phase 4 | Realms / SPL Governance |

**결론:** Phase 1~2에서는 커스텀 Solana Program이 **불필요**하다. Metaplex SDK + 오프체인 스크립트로 충분히 구현 가능. Phase 3 이후 온체인 거버넌스가 필요할 때 검토.

### Metaplex NFT 표준 활용 방안

- **Bubblegum (cNFT):** 대량 에어드롭에 최적. Merkle Tree 기반.
- **Token Metadata:** 메타데이터 표준 준수
- **Collection NFT:** 모든 응원 NFT를 하나의 컬렉션으로 묶음
- **Candy Machine v3:** 필요 시 퍼블릭 민트 이벤트에 활용

### Compressed NFT (cNFT) 상세

```
[Merkle Tree 생성]
  ├── maxDepth: 20         (최대 2^20 = 1,048,576 잎 노드)
  ├── maxBufferSize: 256
  ├── canopyDepth: 14      (프루프 크기 감소)
  └── 생성 비용: ~1.5 SOL (1회성)

[cNFT 민트]
  ├── 잎 노드 추가: ~0.00001 SOL/개
  ├── 배치 처리: 1 트랜잭션에 최대 5개
  └── 일일 1000개 기준: ~0.01 SOL/일
```

**장점:**
- 1백만 개 NFT 민트 비용: ~10 SOL (일반 NFT 대비 10,000 SOL 절약)
- Tensor, Magic Eden 등 주요 마켓플레이스에서 지원

### 토큰 게이팅 로직

```typescript
async function verifyHolder(walletAddress: string): Promise<boolean> {
  const balance = await getTokenBalance(walletAddress, OTTOGI_MINT);
  return balance > 0;
}

async function getHolderTier(walletAddress: string): Promise<string> {
  const balance = await getTokenBalance(walletAddress, OTTOGI_MINT);
  const holdingDays = await getHoldingDuration(walletAddress);
  
  if (balance >= 10_000_000) return 'diamond';
  if (balance >= 1_000_000)  return 'gold';
  if (balance >= 100_000)    return 'silver';
  if (balance > 0)           return 'bronze';
  return 'none';
}
```

---

## 6. 웹사이트 구조

### 랜딩 페이지 구성

```
ottogi.fun/
├── Hero Section
│   ├── 오뚜기 캐릭터 애니메이션 (넘어졌다 일어남)
│   ├── 슬로건: "넘어져도 다시 일어나는 코인 🎎"
│   ├── [Buy on Raydium] [Join Telegram] CTA 버튼
│   └── 실시간 가격 + 시총
│
├── About Section
│   ├── 프로젝트 소개
│   ├── 왜 오뚜기인가?
│   └── 팀 비전
│
├── NFT Gallery Section
│   ├── 최근 에어드롭된 NFT 미리보기
│   ├── 등급별 필터
│   └── [내 NFT 보기] (월렛 연결)
│
├── Live Airdrop Feed
│   ├── 실시간 에어드롭 로그 (WebSocket)
│   ├── "🎎 Wallet 7x...3k received a 🟡 Legendary NFT!"
│   └── 등급별 통계 차트
│
├── Tokenomics Section
│   ├── 분배 구조 파이차트
│   ├── 본딩커브/LP 상태
│   └── Contract Address (복사 가능)
│
├── Roadmap Section
│   └── 타임라인 UI
│
└── Footer
    ├── 소셜 링크 (Telegram, X, GitHub)
    ├── Contract: Copy 버튼
    └── "Made with ❤️ by Ottogi Team"
```

### 기술 스택 (웹)

- **프레임워크:** Next.js 14 (Static Export)
- **스타일:** Tailwind CSS
- **월렛 연결:** `@solana/wallet-adapter-react`
- **데이터:** Helius DAS API (cNFT 조회)
- **배포:** GitHub Pages (정적) 또는 Vercel

### NFT 갤러리

- 월렛 연결 → DAS API로 해당 월렛의 Ottogi cNFT 조회
- 등급별 필터, 시간순 정렬
- 각 NFT 클릭 시 상세 정보 (메시지, 등급, 에어드롭 일시)
- 공유 기능 (X/텔레그램으로 공유)

### 실시간 에어드롭 로그

- WebSocket 또는 SSE로 실시간 피드
- 최근 100건 표시
- 등급별 이모지 + 월렛 주소 (축약) + 타임스탬프

### 등급별 통계

- 전체 발행된 NFT 수
- 등급별 비율 (실제 vs 이론)
- 가장 많은 NFT를 보유한 월렛 TOP 10
- 일/주/월별 에어드롭 수

### GitHub Pages 배포

```bash
# next.config.js
const nextConfig = {
  output: 'export',
  basePath: '/ottogi',
  images: { unoptimized: true },
};

# 배포
npm run build
npx gh-pages -d out
```

---

## 7. 커뮤니티 전략

### 텔레그램 그룹 운영

- **메인 채팅:** 한국어/영어 혼용
- **공지 채널:** 에어드롭 결과, 업데이트, 로드맵 진행
- **규칙:** 스캠 방지 봇, 링크 제한, 신규 멤버 캡차
- **이벤트:** 주간 밈 대회, AMA, 커뮤니티 투표

### 텔레그램 봇

```
/balance <wallet>     → 보유량 확인
/mynft               → 내 NFT 컬렉션 (월렛 연결 필요)
/stats               → 전체 통계 (보유자 수, NFT 발행량)
/nextdrop            → 다음 에어드롭까지 남은 시간
/grade               → 내 등급 확인 (보유량 기반)
/leaderboard         → 보유량 TOP 10
```

**구현:** `grammY` 또는 `node-telegram-bot-api` + Helius DAS API

### X (트위터) 마케팅 전략

1. **밈 콘텐츠:** 오뚜기 캐릭터 + 크립토 밈 조합
2. **에어드롭 알림:** 매 에어드롭마다 자동 트윗
3. **커뮤니티 RT 이벤트:** RT + 팔로우 → 추가 NFT 에어드롭
4. **KOL 협업:** 솔라나 밈코인 인플루언서 접촉
5. **해시태그:** `#OTTOGI` `#넘어져도다시` `#OttogiCoin` `#SolanaMeme`

### 밈 콘텐츠 계획

- 오뚜기 캐릭터 시리즈 (다양한 상황에서 넘어졌다 일어남)
- 시장 하락 시 "오뚜기 타임" 밈 자동 생성
- GIF/숏폼 영상 (오뚜기 넘어지고 일어나는 애니메이션)
- 커뮤니티 밈 대회 (주간, 우승자 Legendary NFT 에어드롭)

---

## 8. 로드맵

### Phase 1: 탄생 (Week 1~2)

- [x] $OTTOGI 토큰 Pump.fun 발행
- [ ] 로고 및 브랜드 아이덴티티 확립
- [ ] 랜딩 페이지 (ottogi.fun) 배포
- [ ] 텔레그램 그룹 + 봇 개설
- [ ] X 계정 개설 + 초기 밈 콘텐츠 10개
- [ ] CoinGecko / DexScreener 등록

### Phase 2: 응원 시작 (Week 3~6)

- [ ] cNFT 시스템 구축 (Merkle Tree 생성, 메시지 DB)
- [ ] 자동 에어드롭 봇 개발 + 테스트
- [ ] 메인넷 에어드롭 시작
- [ ] NFT 갤러리 페이지 오픈
- [ ] 텔레그램 봇 고도화

### Phase 3: 성장 (Month 2~4)

- [ ] 커뮤니티 1,000+ 홀더 달성
- [ ] DEX Screener 트렌딩 진입
- [ ] KOL 마케팅 캠페인
- [ ] NFT 마켓플레이스 연동 (Tensor, Magic Eden)
- [ ] 커뮤니티 거버넌스 투표 시스템

### Phase 4: 확장 (Month 4+)

- [ ] 멀티체인 확장 (Base, Ethereum L2)
- [ ] 파트너십 (다른 밈코인, NFT 프로젝트)
- [ ] 실물 굿즈 (오뚜기 피규어, 스티커)
- [ ] 모바일 앱 (NFT 월렛 + 응원 알림)
- [ ] DAO 전환

---

## 9. 비용 상세

### 항목별 예상 비용

| 항목 | 비용 (SOL) | 비용 (USD, SOL=$150 기준) | 비고 |
|------|-----------|--------------------------|------|
| Pump.fun 토큰 발행 | 0.02 | $3 | 발행 수수료 |
| Merkle Tree 생성 (depth 20) | 1.5 | $225 | 1회성, 100만 NFT 용량 |
| cNFT 민트 (월 3만 개) | 0.3/월 | $45/월 | ~0.00001 SOL/개 |
| Arweave 메타데이터 저장 | 0.1/월 | $15/월 | JSON 메타데이터 |
| AI 이미지 생성 (Mythic) | - | $12/월 | DALL-E 3, 약 300개/월 |
| Helius RPC (Pro) | - | $49/월 | DAS API + Enhanced RPC |
| 도메인 (ottogi.fun) | - | $15/년 | .fun 도메인 |
| 서버 (Railway/VPS) | - | $5/월 | 에어드롭 봇 호스팅 |
| Solana RPC (QuickNode) | - | $0 (무료) | Free tier 충분 |
| **초기 합계** | **~2 SOL** | **~$300** | 1회성 비용 |
| **월간 운영** | **~0.5 SOL** | **~$126/월** | 반복 비용 |

### SOL 필요량 계산

```
초기 셋업:        2 SOL
운영 6개월:       3 SOL  (0.5 SOL × 6)
마케팅 예비:      2 SOL
예비비:           3 SOL
─────────────────────────
총 필요:         ~10 SOL (~$1,500)
```

---

## 10. 리스크 분석

### 기술적 리스크

| 리스크 | 심각도 | 확률 | 대응 |
|--------|--------|------|------|
| Solana 네트워크 다운타임 | 중 | 낮 | 에어드롭 큐잉 후 재시도 |
| RPC 레이트 리밋 | 중 | 중 | 다중 RPC 엔드포인트 (Helius + QuickNode + Public) |
| Merkle Tree 용량 초과 | 낮 | 낮 | 새 트리 생성 (depth 20 = 100만 개) |
| 에어드롭 봇 장애 | 중 | 중 | 모니터링 + 자동 재시작 + 텔레그램 알림 |
| 메타데이터 스토리지 장애 | 낮 | 낮 | Arweave (영구 저장) 사용 |

### 규제 리스크

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| 증권성 판단 | 높 | 수익 약속 없음, 유틸리티(NFT) 강조, 탈중앙 구조 |
| 한국 가상자산법 | 중 | 한국 거래소 상장 지양, 글로벌 DEX 중심 |
| NFT 규제 | 낮 | 무료 에어드롭, 판매 아님 |
| 세금 문제 | 중 | 에어드롭 수령 시 과세 가능성 고지 |

### 시장 리스크

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| 밈코인 시장 냉각 | 높 | 유틸리티(NFT)로 차별화, 장기 보유 인센티브 |
| 유동성 부족 | 높 | 초기 유동성 확보 + LP 소각으로 신뢰 구축 |
| 경쟁 프로젝트 | 중 | 독특한 컨셉(응원/격려) + 커뮤니티 충성도 |
| SOL 가격 하락 | 중 | 운영비 스테이블코인 일부 보유 |

---

## 11. 기술 스택 정리

### 언어 및 프레임워크

| 분류 | 기술 | 용도 |
|------|------|------|
| 언어 | TypeScript | 메인 개발 언어 |
| 언어 | Python | AI 이미지 생성, 데이터 분석 |
| 프론트엔드 | Next.js 14 | 웹사이트 |
| 스타일링 | Tailwind CSS | UI |
| 백엔드 | Node.js 20+ | 에어드롭 봇, 텔레그램 봇 |
| DB | SQLite / PostgreSQL | 메시지 DB, 에어드롭 로그 |

### 블록체인 SDK

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| `@solana/web3.js` | 2.x | Solana 기본 상호작용 |
| `@metaplex-foundation/mpl-bubblegum` | latest | cNFT 민트 |
| `@metaplex-foundation/mpl-token-metadata` | latest | NFT 메타데이터 |
| `@metaplex-foundation/umi` | latest | Metaplex 통합 프레임워크 |
| `@solana/spl-token` | latest | SPL 토큰 조작 |

### 외부 API

| API | 용도 | 비용 |
|-----|------|------|
| Helius DAS API | cNFT 조회, 보유자 스캔 | $49/월 (Pro) |
| Helius Enhanced RPC | 트랜잭션 | 포함 |
| OpenAI DALL-E 3 | Mythic 등급 이미지 생성 | $0.04/image |
| Arweave (Irys) | 메타데이터 영구 저장 | ~$0.001/upload |
| CoinGecko API | 가격 정보 | 무료 |
| Birdeye API | DEX 가격/차트 | 무료 |

### 도구

| 도구 | 용도 |
|------|------|
| `node-cron` | 에어드롭 스케줄링 |
| `grammY` | 텔레그램 봇 |
| `sharp` | 이미지 처리 (텍스트 오버레이) |
| `winston` | 로깅 |
| `dotenv` | 환경변수 관리 |
| `gh-pages` | GitHub Pages 배포 |

### 개발 환경 요구사항

```
- Node.js >= 20.0.0
- npm >= 10.0.0
- Solana CLI >= 1.18
- Anchor (선택, Phase 4)
- Git
- VS Code + Solana 확장

환경변수:
  SOLANA_RPC_URL=https://rpc.helius.xyz/?api-key=xxx
  HELIUS_API_KEY=xxx
  OTTOGI_MINT=<토큰 민트 주소>
  AUTHORITY_KEYPAIR=<경로>
  OPENAI_API_KEY=xxx
  TELEGRAM_BOT_TOKEN=xxx
  ARWEAVE_WALLET=<경로>
  DATABASE_URL=sqlite:./data/ottogi.db
```

---

## 부록: 프로젝트 디렉토리 구조

```
ottogi/
├── apps/
│   ├── web/                 # Next.js 웹사이트
│   ├── airdrop-bot/         # 에어드롭 자동화 봇
│   └── telegram-bot/        # 텔레그램 봇
├── packages/
│   ├── core/                # 공유 유틸리티 (등급 결정, DB 등)
│   ├── solana/              # Solana 상호작용 래퍼
│   └── types/               # 공유 타입 정의
├── data/
│   ├── messages/            # 응원 메시지 JSON/CSV
│   ├── images/              # 템플릿 이미지
│   └── ottogi.db            # SQLite DB
├── scripts/
│   ├── setup-merkle-tree.ts
│   ├── seed-messages.ts
│   └── test-airdrop.ts
├── docs/
│   └── TECHNICAL_SPEC.md    # 이 문서
├── .env.example
├── package.json
├── turbo.json               # Turborepo 모노레포
└── README.md
```

---

*이 문서는 $OTTOGI 프로젝트의 기술 설계 기준 문서입니다. 개발 진행에 따라 업데이트됩니다.*

*Last updated: 2026-02-19*
